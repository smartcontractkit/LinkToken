{
  "name": "@chainlink/token",
  "homepage": "https://github.com/smartcontractkit/LinkToken",
  "repository": "https://github.com/smartcontractkit/LinkToken",
  "version": "0.1.0",
  "license": "MIT",
  "authors": [
    "Steve Ellis <steve@smartcontract.com>"
  ],
  "files": [
    "README.md",
    "contracts"
  ],
  "scripts": {
    "clean": "tsc -b --clean tsconfig.test.json && rm -rf build/",
    "compile": "belt compile all",
    "compile:clean": "yarn clean & yarn compile",
    "setup": "yarn compile:clean",
    "test": "jest --testTimeout 80000 --forceExit --detectOpenHandles"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.1.6",
    "@openzeppelin/contracts": "^3.4.0"
  },
  "devDependencies": {
    "@chainlink/belt": "^0.0.3",
    "@chainlink/test-helpers": "^0.0.5",
    "@types/jest": "^26.0.8",
    "@types/node": "^13.9.1",
    "jest": "^26.6.3",
    "jest-circus": "^25.1.0",
    "moment": "^2.15.2",
    "prettier": "^1.19.1",
    "ts-jest": "^26.5.3",
    "typescript": "^3.9.7"
  },
  "resolutions": {
    "**/ethers": "4.0.45"
  }
}
